1. We will add `currency-lab` to manage real-time and static currency conversions (via static fallback or mock in tests to avoid needing API keys). This tool does not exist!
Wait, `finance_lab.py` exists.

Let's do `math-lab`? `math_lab.py` exists.
`morse-lab`? `morse_lab.py` exists.

How about `braille-lab`?
A tool to translate text to Braille patterns and vice versa!
It does not exist.

Features:
- **CLI Commands**:
  - `braille-lab encode "text"`: Translates text to Braille dots.
  - `braille-lab decode "⠞⠑⠭⠞"`: Translates Braille back to text.
  - `braille-lab tui`: Interactive Textual UI.
- **TUI Tab**: A tab `tab-braille` with two text areas for live encoding/decoding.
