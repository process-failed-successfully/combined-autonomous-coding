[project]
name = "combined-autonomous-coding"
version = "0.1.0"
description = "Combined Autonomous Coding Agent"
authors = [
    {name = "User", email = "user@example.com"},
]
dependencies = []
requires-python = ">=3.11"

[tool.bandit]
exclude_dirs = ["tests", "tests_output"]
